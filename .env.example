# InsForge backend (read by @insforge/sdk/ssr's default env resolution)
NEXT_PUBLIC_INSFORGE_URL=
NEXT_PUBLIC_INSFORGE_ANON_KEY=

# Gemini API (server-only; never exposed with a NEXT_PUBLIC_ prefix)
GEMINI_API_KEY=
# Optional. Defaults to gemini-2.5-flash if unset.
GEMINI_MODEL=
